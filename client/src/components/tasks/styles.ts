import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  container: {
    textAlign: "center",
  },
  text: {
    fontSize: 20,
    color: "#D2D4D4",
  },
}));

export default useStyles;
