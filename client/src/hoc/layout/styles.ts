import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {
    marginTop: "140px",
    marginLeft: "100px",
    marginRight: "100px",
  },
}));

export default useStyles;
